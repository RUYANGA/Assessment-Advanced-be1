# Django secret key
SECRET_KEY=django-insecure-l+qsyp&5ef!o$y4@$ad$6=nnssp)lcg#@+4lru4+knv(y6mrtl

# Debug mode (False for production)
DEBUG=False

# Allowed hosts (your Render backend domain)
ALLOWED_HOSTS=0.0.0.0:8001

# Database configuration
DB_NAME=db_name
DB_USER=db_user
DB_PASSWORD=db_password
DB_HOST=db_host
DB_PORT=db_port

# Email (SMTP) configuration
EMAIL_BACKEND=django.core.mail.backends.smtp.EmailBackend
EMAIL_HOST=smtp.gmail.com
EMAIL_PORT=587
EMAIL_USE_TLS=True
EMAIL_HOST_USER=your_email@gmail.com
EMAIL_HOST_PASSWORD=your_email_password
DEFAULT_FROM_EMAIL=your_email@gmail.com

# Session management
ACCOUNT_SESSION_REMEMBER=None

# CSRF trusted origins (frontend + backend)
CSRF_TRUSTED_ORIGINS=frontend_url + backend_url

# CORS allowed origins (frontend only)
CORS_ALLOWED_ORIGINS=frontend_url
CORS_ALLOW_CREDENTIALS=True